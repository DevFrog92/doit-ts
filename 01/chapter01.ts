// 1. type inference
let num_01 = 1;

// number -> type annotation
let num: number = 1;

// 2. interface
interface Person {
  name: string
  age?: number
}

// 3. tuple
let tuple: [boolean, number, string] = [false, 1, '1'];
